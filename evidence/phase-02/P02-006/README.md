# P02-006 Code and Dependency Policy Evidence

- Task: `P02-006` — configure strict formatting, linting, warnings, unsafe-code review, dependency-policy, and license checks
- Requirements supported: `INV-003`, `INV-004`, `INV-006`, `INV-007`, `CORE-001`, `CORE-003`, `SEC-001`, `SEC-002`, `QUAL-001`
- Commit under test: `5d3fe25349635e19a1863f1ee72293564eba0170`
- Recorded at: `2026-07-11T00:14:21Z`
- Recorder: Codex implementation agent
- Reviewer: pending independent `G02` gate review
- Verdict: Pass

## Scope

This step makes the bootstrap code-quality and dependency boundaries executable:

- all eight Rust crates inherit warnings/future/2024/missing-docs/unreachable-public errors, forbidden unsafe code, Clippy `all`/`pedantic`, and selected restriction lints;
- rustfmt, native/Wasm Clippy, warning-free rustdoc, and ASan-target compilation are clean;
- Biome 2.5.3 is exact-pinned and checks 29 active JavaScript/TypeScript/JSON source/configuration files;
- TypeScript strict checks remain mandatory;
- Cargo/npm sources, exact versions, integrity, runtime scope, licenses, lifecycle scripts, duplicates, build scripts, and unsafe tokens are reconciled by one machine policy; and
- the third-party notice now matches the complete locked development-tool inventory.

The normative details and exception workflow are in [Code Quality, Unsafe Review, and Dependency Policy](../../../docs/architecture/code-quality-and-dependency-policy.md).

## Rust results

- Workspace packages: 8 unpublished MIT path packages; 0 external crates.
- Workspace lint inheritance: 8 of 8 manifests.
- Compiler warning policy: `warnings`, `future_incompatible`, and `rust_2024_compatibility` denied.
- Unsafe policy: compiler `forbid`, 8 candidate `.rs` files scanned, 0 unsafe tokens, 0 build scripts, 0 exceptions.
- Clippy: `all` + `pedantic` denied; `unwrap`, `expect`, panic, unfinished/debug/print macros, and unexplained allow attributes denied.
- Native format/check/Clippy/test/rustdoc: pass; 9 all-feature tests.
- Portable target Clippy: `wasm32-unknown-unknown` and `wasm32-wasip2` pass for the `helix-core` closure.
- ASan target all-target/all-feature check: pass.

Verifier canaries inject an unsafe function and a warning into a temporary source checkout. Both compilation and the independent candidate-source scanner reject the mutations; the original source is restored before normal checks continue.

## JavaScript and semantic results

- Biome inventory: 29 active files, 0 warnings/errors/format/import drift.
- Exact formatter policy: LF, two spaces, 100 columns, single quotes, semicolons, and trailing commas.
- Exact lint additions: `node:` built-ins and no explicit TypeScript `any`, with recommended rules elevated by `--error-on-warnings`.
- TypeScript root/tool configuration: pass on Node 22.23.1 and 24.18.0.
- Biome/lock/dependency policy: pass on both Node lanes after clean script-suppressed installs; lock hash unchanged.
- Negative formatter/linter canary: an untracked active source with `var`/double quotes is rejected.
- Semantic examples/corpus: 4 schema-valid examples, 3 exact negative examples, 17 fixtures, 313 steps, and stable generated bytes.
- Oracle: 382 unit/property/negative assertions; 313 corpus steps pass with 0 failed/skipped.
- Compatibility: 263-row matrix, 16 MongoDB overlap cases, mutation canaries, and stable generated document.
- MongoDB artifacts: 3 schemas, 16 cases, 4 offline mutation canaries.
- Live pinned MongoDB 6.0.5 differential: 16/16 pass (12 exact, 4 deliberate differences), expectation mutation canary detected, 0 residual containers.

The source commit changes no generated semantic fixture, oracle report, compatibility matrix/report, or MongoDB observation/report. The behavior-sensitive JavaScript changes are formatting/import organization plus reviewed lint-equivalent rewrites, and all executable authorities above pass from the immutable source commit.

## Dependency and license results

- npm lock: version 3; 91 development-only packages; 91 canonical registry resolutions and SHA-512 integrity records; 0 production/optional root dependencies.
- Direct development tools: 6 exact versions.
- License forms: MIT 60, MIT OR Apache-2.0 9, Apache-2.0 6, MPL-2.0 12, ISC 2, BSD-3-Clause 1, 0BSD 1.
- Build-only exception: exactly 12 `lightningcss` 1.32.0 family packages under MPL-2.0, barred from shipped contents pending `P02-012`/release revalidation.
- Lifecycle scripts: exactly two optional Darwin `fsevents` entries (2.3.2 and 2.3.3), inventoried and explicitly denied; deterministic installs suppress scripts.
- Duplicate versions: exactly the reviewed `fsevents` pair.
- Sources: no git/file/link/alternate-registry npm source; no external/git Rust source.

Verifier mutation canaries independently replace a lock license with GPL-3.0-only, change a registry URL, and add an unreviewed lifecycle script. The policy checker rejects each exact mutation before the source is restored.

This is deterministic lock/license screening, not a vulnerability-free claim, full license-text/provenance report, SBOM, or legal opinion. Those retained reports remain `P02-012` and release work.

## Environment

```text
OS: Linux 6.8.0-124-generic
architecture: x86_64
rustc: 1.96.1 (31fca3adb 2026-06-26)
cargo: 1.96.1 (356927216 2026-06-26)
active Node.js: v22.19.0
Corepack: 0.34.0
npm through Corepack: 11.18.0
Biome: 2.5.3
JavaScript validation lanes: Node 22.23.1 and 24.18.0
MongoDB image: mongo@sha256:161fdedb41a151ec56efa544b3f8a99d0f7d3f63136d1b64891d22bea487c417
MongoDB server: 6.0.5
mongosh: 1.8.0
```

## Commands

```bash
cargo fmt --all -- --check
cargo check --frozen --workspace --all-targets --all-features
cargo clippy --frozen --workspace --all-targets --all-features -- -D warnings
RUSTDOCFLAGS="-D warnings" cargo doc --frozen --workspace --no-deps --all-features
cargo clippy --frozen --target wasm32-unknown-unknown --package helix-core -- -D warnings
cargo clippy --frozen --target wasm32-wasip2 --package helix-core -- -D warnings
cargo check --frozen --target x86_64-unknown-linux-gnuasan --workspace --all-targets --all-features
corepack npm ci --ignore-scripts
corepack npm run policy:javascript
corepack npm run policy:dependencies
corepack npm run toolchain:types
node fixtures/semantic/generate-corpus.mjs --check
node fixtures/semantic/check-corpus.mjs
node reference/semantic-oracle/test-oracle.mjs
node reference/semantic-oracle/cli.mjs --check-report
node compatibility/v1/generate-matrix.mjs --check
node compatibility/v1/check-matrix.mjs
node differential/mongodb/check-artifacts.mjs
node differential/mongodb/run.mjs --check-report
node differential/mongodb/run.mjs --canary-expected-relation
node evidence/phase-02/P02-006/verify.mjs 5d3fe25349635e19a1863f1ee72293564eba0170
```

## Retained diagnostic attempts

1. The first Biome check exited 1 with 43 errors, 4 warnings, and 5 informational diagnostics across the pre-policy source. Biome safely formatted/organized 20 files; remaining diagnostics were reviewed and fixed narrowly. No generated data was rewritten.
2. The first strict Clippy pass exited 101 on `clippy::doc_markdown` for an unquoted `HelixDB` identifier. The documentation token was corrected; the complete strict pass then succeeded.
3. A read-only exploratory `npm install-scripts ls --all --json` exited 1 because npm 11.18 forbids `--all` with `ls`. The documented `npm install-scripts ls --json` form passed and reported no approved script; the immutable lock checker remained authoritative throughout.
4. The first clean-room evidence replay exited 1 because `git archive` omits repository metadata while the dependency scanner intentionally inventories tracked and untracked candidates through `git ls-files`. The verifier now creates a temporary Git index immediately after extraction; the scanner and source policy were not weakened.

These attempts changed no accepted artifact outside the reviewed P02-006 source commit and are retained so a green result does not hide policy-bootstrap failures.

## Limitations

This task does not add time-dependent vulnerability/advisory queries, package provenance attestations, full upstream license-text collection, SBOM generation, CI jobs, real package-content reconciliation, Miri/fuzz execution, or an unsafe exception. Those remain later named tasks. Strict static policy also does not prove semantic correctness, memory safety, security, or production readiness by itself.
