# Browser Tests

Reserved for real Chromium, Firefox, and WebKit lifecycle, capability, bundle, storage, quota, and fallback tests. `npm run test:browser` currently performs a binary-free Playwright inventory and requires exactly zero tests; real execution activates under `P02-010` and `P02-016`. No browser binary or database bundle is installed yet.
