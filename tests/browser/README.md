# Browser Tests

Reserved for real Chromium, Firefox, and WebKit lifecycle, capability, bundle, storage, quota, and fallback tests. The `P02-009` Playwright configuration names all three projects, while `npm run test:browser` and the per-engine CI commands perform binary-free inventories and require exactly zero tests. Real execution activates under `P02-010` and `P02-016`. No browser binary or database bundle is installed yet.
