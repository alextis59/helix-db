# Fuzz Tests

Reserved for version-pinned, coverage-guided targets, seed corpora, dictionaries, bounded smoke invocations, and retained crash reproducers. The stable `npm run test:fuzz` command reports zero targets until codec fuzzing is introduced under `P03-019`; property tests are not mislabeled as coverage-guided fuzzing.
