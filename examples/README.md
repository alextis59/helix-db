# Examples

This root is reserved for minimal, executable native, browser, server, and SDK examples. Each example must state the exact feature and maturity it proves and link to reproducible evidence.

No example exists yet. The first non-functional native/browser toolchain examples are owned by `P02-016` and must not imply database functionality.
